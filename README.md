# Mini‑Storefront (Next.js App Router)


A small storefront built with **Next.js (App Router)** and **Tailwind CSS**. 


## Setup
```bash
npx create-next-app@latest mini-storefront
# Answers: No TS, Yes ESLint, Yes Tailwind, Yes src/, Yes App Router, No alias
cd mini-storefront
# Replace files under src/app with the files in this repo
npm install
npm run dev
# visit http://localhost:3000
```

Rubric Checklist
- Project setup & structure
- Components + JSX + stable keys
- Props + lifting state
- State + controlled inputs
- Effects + cleanup
- UX + conditional rendering
